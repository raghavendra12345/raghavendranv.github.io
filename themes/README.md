PaperMod goes here. From the project root, run:

    git submodule add --depth=1 \
      https://github.com/adityatelange/hugo-PaperMod.git themes/PaperMod

The deploy workflow checks out submodules recursively and expects this
exact path. Delete this file afterwards if you like.
